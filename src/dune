(ocamllex
  (modules lexer)
)

(menhir
  (modules parser)
  (flags ("-la" "2" "--explain" "--trace"))
)

; add  "--trace" "--explain" to flags in order to debug


(library
    (name cast)
    (libraries threads cduce str)
    (modules parser syntax types primitives lexer print interpreter compile exec))
