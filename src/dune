(ocamllex
  (modules lexer)
)

(menhir
  (modules parser)
  (flags ("-la" "2"))
)

; add  "--trace" "--explain" to flags in order to debug


(library
    (name cast)
    (libraries threads cduce)
    (modules parser syntax types primitives lexer print interpreter))
