(ocamllex
  (modules lexer)
)

(menhir
  (modules parser)
  (flags ("-la" "2" "--trace" "--explain"))
)


(library
    (name cast)
    (libraries threads cduce)
    (modules parser syntax types primitives lexer print))
